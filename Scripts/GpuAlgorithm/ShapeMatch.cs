using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class ShapeMatch
{
    
    public static List<Vector3> RoundMatch(List<Vector3> points)
    {
        if (points.Count % 3 != 0)
        {
            
        }
        return null;
    }
}
